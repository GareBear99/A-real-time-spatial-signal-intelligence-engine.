# OPS V7

This version adds a local-only operations layer on top of the v6 inference core:

- Gateway adapters: mock, JSON, CSV, NMEA-ish
- Operator roster
- Incident workflow
- Evidence export

It is intended as a strong local prototype for future backend/API work.
